#!/bin/sh
set -eu
ROOT="/opt/autocrack/toolpacks/active/capa"
export PYTHONPATH="$ROOT/python${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONDONTWRITEBYTECODE=1
exec /usr/bin/python3 -B -c '
import sys
sys.argv[0] = "capa"
from capa.main import main
raise SystemExit(main())
' "$@"
