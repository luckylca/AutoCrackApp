#!/bin/sh
set -eu
SELF_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PACK_ROOT=$(CDPATH= cd -- "$SELF_DIR/.." && pwd)
export PYTHONPATH="$PACK_ROOT/python${PYTHONPATH:+:$PYTHONPATH}"
exec /usr/bin/python3 "$PACK_ROOT/libexec/frida_autocrack_client.py" "$@"
