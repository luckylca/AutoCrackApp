# android-lldb-server toolpack

Packages the pinned Android ARM64 `lldb-server` used by AutoCrackApp's loopback debugger bridge. Runtime access remains typed: no raw gdb-remote packets, register writes, memory writes, software breakpoints, or arbitrary target switching are exposed.
